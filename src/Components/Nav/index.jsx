import React from 'react';
import { StyledNav, NavTitle } from './styles';

const Nav = () => {
  return (
    <StyledNav>
      <NavTitle>News Api</NavTitle>
    </StyledNav>
  );
};

export default Nav;
